node_modules
dist
.angular
.turbo
.env
.env.local
coverage
*.log
*.tsbuildinfo
packages/database/generated
graphify-out/

# A stray clone of this same repository that predates this session.
# Committing it as an embedded git repo once already; never again.
/MOP/

scratch/
